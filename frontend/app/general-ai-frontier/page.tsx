import SectionPage from "../_components/section-page";

export default function GeneralAIFrontierPage() {
  return (
    <SectionPage
      title="General AI Frontier"
      description="LLM, MLLM, reasoning, agent systems, alignment, and evaluation."
      query="llm mllm reasoning agent rag alignment evaluation"
    />
  );
}
